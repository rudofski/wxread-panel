# 本地开发无需任何环境变量，直接 npm run dev。
#
# 书城搜索 CORS 代理（仅 GitHub Pages 线上需要）：
# weread.qq.com 未开放 CORS，线上部署时经自建 Cloudflare Worker 转发。
# 本地开发不设置即直连；线上通过 GitHub Actions Variables 的
# VITE_WEREAD_PROXY 注入（见 worker/README.md）。
# VITE_WEREAD_PROXY=https://wxread-search-proxy.<你的子域>.workers.dev
